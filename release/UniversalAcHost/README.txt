Universal ESP32 AC Remote Windows package

Double-click UniversalAcHost.bat to start the Qt host.

The application executable and its Qt/MSYS2 runtime DLLs live in bin so that
Windows can find the required DLLs while keeping this folder tidy.
